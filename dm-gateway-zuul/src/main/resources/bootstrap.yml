spring:
  application:
    name: dm-gateway-zuul
# config-server url
# param: application name + profile version + branch
  cloud:
    config:
      uri: http://localhost:7900
      profile: prod
      lable: BaseEnv
# sleuth + zipkin
  sleuth:
    sampler:
      percentage: 1.0
  zipkin:
    base-url: http://zipkin:7800